{

  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index.js "
    
  }, 
  "dependencies": {
    "node-telegram-bot-api": "^0.57.0",
    "nodemon": "^2.0.16"
  }
}

